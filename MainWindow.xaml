<Window x:Class="ShipnetOnline.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wf="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="Shipnet Online" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- RDP surface -->
        <wf:WindowsFormsHost x:Name="RdpHost" Grid.Row="0" />

        <!-- Placeholder shown before a connection is established / on failure -->
        <TextBlock x:Name="PlaceholderText"
                   Grid.Row="0"
                   Text="Connecting to Shipnet..."
                   Foreground="White" FontSize="16"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Status bar -->
        <Border Grid.Row="1" Background="#0D2A4A" Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusText" Grid.Column="0"
                           Text="Idle" Foreground="White" VerticalAlignment="Center" />

                <TextBlock x:Name="HealthScoreText" Grid.Column="1"
                           Text="Health: --" Foreground="#5AA0DC"
                           Margin="0,0,16,0" VerticalAlignment="Center" />

                <Button x:Name="DisconnectButton" Grid.Column="2"
                        Content="Disconnect" Click="DisconnectButton_Click"
                        Padding="12,4" Background="#C62828" Foreground="White" BorderThickness="0" />
            </Grid>
        </Border>
    </Grid>
</Window>
